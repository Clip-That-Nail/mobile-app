import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const PawImage = ({ pawData }) => {
  return (
    <View></View>
  );
};

const styles = StyleSheet.create({});

export default PawImage;