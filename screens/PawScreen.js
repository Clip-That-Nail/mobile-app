import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const PawScreen = (props) => {
  return (
    <View><Text>Paw Screen</Text></View>
  );
};

const styles = StyleSheet.create({});

export default PawScreen;