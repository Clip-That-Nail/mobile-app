class Pet {
  constructor(id, name, type, breed, imageUri, disabled) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.breed = breed;
    this.imageUri = imageUri;
    this.disabled = disabled;
  }
}

export default Pet;