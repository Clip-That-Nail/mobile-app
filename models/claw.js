class Claw {
  constructor(status = 'unchecked', outcome = 'not-selected', behaviour = 'not-selected') {
    this.status = status;
    this.outcome = outcome;
    this.behaviour = behaviour;
  }
}

export default Claw;